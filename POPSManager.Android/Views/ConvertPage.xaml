<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="POPSManager.Android.Views.ConvertPage"
             Title="Convertir Juegos"
             BackgroundColor="{StaticResource BackgroundPrimary}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="18">

            <Label Text="📁 Origen" Style="{StaticResource SectionTitle}" />
            <Entry Text="{Binding SourceFolder}" Placeholder="Carpeta de origen" Style="{StaticResource DarkEntry}" />
            <Button Text="Seleccionar origen" Style="{StaticResource PrimaryButton}" ImageSource="folder_open.png" />

            <Label Text="📁 Destino" Style="{StaticResource SectionTitle}" />
            <Entry Text="{Binding DestFolder}" Placeholder="Carpeta de destino" Style="{StaticResource DarkEntry}" />
            <Button Text="Seleccionar destino" Style="{StaticResource PrimaryButton}" ImageSource="folder_open.png" />

            <Button Text="🔄 Convertir archivos" Style="{StaticResource PrimaryButton}" />
            <Button Text="📂 Abrir carpeta de salida" Style="{StaticResource SecondaryButton}" />

            <Label Text="{Binding Status}" Style="{StaticResource StatusLabel}" />

            <CollectionView ItemsSource="{Binding Files}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="{Binding Icon}" FontSize="14" VerticalOptions="Center" />
                            <Label Text="{Binding Name}" TextColor="{StaticResource TextPrimary}" VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>